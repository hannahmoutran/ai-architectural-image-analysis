# AI Provider SDKs
openai>=1.0.0
anthropic>=0.30.0
google-genai>=1.0.0
portkey-ai>=1.0.0  # Optional: only required if OPENAI_USE_PORTKEY = True in config.py

# Data processing
pandas
openpyxl
lxml
rapidfuzz

# HTTP & async
requests
aiohttp
tenacity

# Image processing
pillow

# NLP & tokenization
spacy
tiktoken